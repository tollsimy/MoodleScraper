-fix all "Path" (unify path for all OS)
-split script in multiple files
-inserire il nome del corso con tutto il nome del json è scomodo
-json mode from -j on command (?)
-youtube upload
-retry if login credentials are wrong
-ci e qualquadra che non cosa con la page di ES